# o11y-operator

A Kubernetes Operator created via [controller-runtime](https://github.com/kubernetes-sigs/controller-runtime), that leveraging OpenTelemetry to gather the three observability signals((traces, metrics, logs) ) stores them in a unified data store.
